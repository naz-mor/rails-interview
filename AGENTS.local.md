# Local Development

All project commands (rails, ruby, bundle, rake, rspec, etc.) must be run using the devcontainer:

```
devcontainer exec --workspace-folder /Users/nazam/Code/rails-interview <command>
```
