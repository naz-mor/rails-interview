# AGENTS

## Local instructions

Before making changes or running project commands, read `AGENTS.local.md` if it exists. The current coding agent (`pi`) does not load it automatically.

## Commits

Use Conventional Commits for all commit messages.

## Feature parity

Keep feature parity between API and APP controllers whenever it's applicable to the API and doesn't break backwards compatibility.
